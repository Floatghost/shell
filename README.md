# About
Blazingly fast 🔥🔥 shell written in Rust 🦀🦀🦀🦀🦀

# Huge Community
since this shell has an huge community (as of writing ts 1 star) there are many plugins that let you extend the shells functionality

# Plugins
plugin support is currently in dev and is expected to release in 2067

# other features
incase you want to test your memory ts shell has an memory tester builtin

you can use it using the command "malloc"

if you want to flex your coolnes you can use the "🐺🗿" command to succeed in life or you can just go to ts [cool website](https://glas-flex.ch/)

# why Rust
because powershell is written in C#
- ❌ C# uses gc which is not optimal for a shell
- ❌ Lacks modern error handling
- ❌ Not memory-safe
- ❌ No fearless concurrency
- ❌ Not written in Rust

## Features ✨

- 🚀 **Blazingly fast** - Outputs your input at unprecedented speeds
- 🛡️ **Memory safe** - No segfaults, guaranteed!
- ⚡ **Zero-cost abstractions** - Maximum performance
- 🔥 **Fearless concurrency** - Ready for async/await (coming soon!)
- 🦀 **100% Rust** - No unsafe code blocks

# usage and download
- first download the .exe from the latest release
- execute the .exe and ignore the warnings
- have fun with the best shell on earth written in rust

# Safety
this program will never crash

if it does that is user error

# Config

    config is in .../APPDATA/Roaming/LeVimShell/shell.toml


# user errors

when using tab some inexperienced users experienced ts:

"sig" -> "🐺🗿" -> "sigverif.exe" -> "🐺🗿  .exe"
